import 'package:flutter/material.dart';

class BurcDetayView extends StatelessWidget {
  
  const BurcDetayView({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text("asdfafa"),),
    );
  }
}